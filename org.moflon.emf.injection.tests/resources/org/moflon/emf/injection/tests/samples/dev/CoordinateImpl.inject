
partial class CoordinateImpl {

@model calculateDistanceTo (Coordinate other) <--
		double deltaX = Math.abs(this.x - other.getX());
		double deltaY = Math.abs(this.y - other.getY());
		return Math.sqrt(deltaX*deltaX + deltaY*deltaY);
-->

}